# Example .env.example for FUEGO-APP
# Do NOT commit your real .env to source control.
#
# Copy this to .env locally and fill with real values, or store values
# in a secrets manager / GitHub Secrets and inject them in CI.
#
# Local development:
# 1) Copy .env.example -> .env
# 2) Fill values with your actual API keys and database credentials
#
# Production / CI:
# - Store these values in your secret store and reference them in your deploy / CI.

# =============================================================================
# Supabase Configuration (Required)
# =============================================================================
# Your Supabase project URL (found in Project Settings > API)
SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon/public key (found in Project Settings > API)
# Used by the Supabase client for client-side operations
SUPABASE_KEY=your-supabase-anon-key-here

# Alternative name for Supabase public key (used in config.js)
SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key-here

# Your Supabase service role key (found in Project Settings > API)
# Required for secure-find-matches endpoint and admin operations
# ⚠️  Keep this secret - it bypasses Row Level Security!
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# Direct PostgreSQL connection string (found in Project Settings > Database)
# Used for running migrations via `npm run migrate`
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project.supabase.co:5432/postgres

# Alternative name for database connection (fallback option)
SUPABASE_DB_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project.supabase.co:5432/postgres

# =============================================================================
# AI Services Configuration
# =============================================================================
# Anthropic Claude API key (get from https://console.anthropic.com/)
# Required for Claude integration in lib/claude.ts
ANTHROPIC_API_KEY=sk-ant-api03-your-anthropic-key-here

# OpenAI API key (get from https://platform.openai.com/api-keys)
# Required for embeddings in lib/embeddings.ts
OPENAI_API_KEY=sk-your-openai-key-here

# AI Gateway API key (alternative to OPENAI_API_KEY)
# Used for match explanations in pages/api/match-explanation.ts
AI_GATEWAY_API_KEY=your-ai-gateway-key-here

# OpenAI model to use for chat completions (optional)
# Default: gpt-4
GPT_MODEL=gpt-4

# OpenAI embedding model (optional)
# Default: text-embedding-3-small
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# =============================================================================
# App Settings (Optional)
# =============================================================================
NODE_ENV=development
PORT=3000
