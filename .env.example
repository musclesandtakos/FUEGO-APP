# Example environment variables — do NOT commit real secrets. Replace placeholders before use.
#
# Create a local file named .env.local (which should be gitignored) and copy the values you
# need from here, replacing REPLACE_WITH_* values with real credentials.

# Primary database connection (Prisma / server-side)
DATABASE_URL="postgresql://postgres:REPLACE_WITH_PASSWORD@db.dvcwwsnntbgjfvdzawwf.supabase.co:5432/postgres"

# Supabase (client-side) — safe to expose the anon key in frontend builds, but treat service role as secret
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="REPLACE_WITH_ANON_KEY"
# Optional (server-only) Supabase key — DO NOT expose in client builds
SUPABASE_SERVICE_ROLE_KEY="REPLACE_WITH_SERVICE_ROLE_KEY"

# Next.js / app URL used by some auth callbacks
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Prisma shadow database (optional, used by some environments/migrations)
# SHADOW_DATABASE_URL="postgresql://postgres:REPLACE_WITH_PASSWORD@shadow-host:5432/postgres"

# Other common entries you may want to include:
# NEXTAUTH_URL="http://localhost:3000"
# NEXTAUTH_SECRET="REPLACE_WITH_A_LONG_RANDOM_SECRET"
