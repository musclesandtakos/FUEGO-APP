# Example .env.example for FUEGO-APP
# Do NOT commit your real .env to source control.
#
# Copy this to .env locally and fill with real values, or store values
# in a secrets manager / GitHub Secrets and inject them in CI.
#
# Local development:
# 1) Copy .env.example -> .env
# 2) Fill values
#
# Production / CI:
# - Store these values in your secret store and reference them in your deploy / CI.

# Prisma / database
PRISMA_DATABASE_URL="postgres://<DB_USER>:<DB_PASS>@<DB_HOST>:<DB_PORT>/<DB_NAME>?sslmode=require"
POSTGRES_URL="postgres://<DB_USER>:<DB_PASS>@<DB_HOST>:<DB_PORT>/<DB_NAME>?sslmode=require"

# Prisma Data Platform / API key (if used)
PRISMA_API_KEY="your-prisma-data-platform-api-key"

# Supabase configuration
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_KEY="your-anon-public-key"
SUPABASE_ANON_KEY="your-anon-public-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-keep-secret"

# Database URL for migrations (use service_role connection string)
DATABASE_URL="postgresql://postgres:[password]@[host]:[port]/[database]"
SUPABASE_DB_URL="postgresql://postgres:[password]@[host]:[port]/[database]"

# App settings
NODE_ENV=development
PORT=3000

# Example other secrets
# SENTRY_DSN=
# NEXTAUTH_SECRET=
