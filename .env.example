# Example environment variables — do NOT commit real secrets. Replace placeholders before use.
#
# Copy this to .env locally and fill with real values, or store values
# in a secrets manager / GitHub Secrets and inject them in CI.

# Database connection (for migrations and server-side operations)
DATABASE_URL="postgresql://postgres:REPLACE_WITH_PASSWORD@your-project.supabase.co:5432/postgres"

# Supabase configuration (used by lib/supabase.ts)
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_KEY="REPLACE_WITH_ANON_KEY"
SUPABASE_PUBLISHABLE_KEY="REPLACE_WITH_ANON_KEY"

# Supabase service role key (required for secure-find-matches and other server-side admin operations) — DO NOT expose in client builds
SUPABASE_SERVICE_ROLE_KEY="REPLACE_WITH_SERVICE_ROLE_KEY"

# Anthropic Claude API (required by lib/claude.ts)
ANTHROPIC_API_KEY="REPLACE_WITH_ANTHROPIC_KEY"

# OpenAI API (required by lib/embeddings.ts and pages/api/match-explanation.ts)
OPENAI_API_KEY="REPLACE_WITH_OPENAI_KEY"
OPENAI_EMBEDDING_MODEL="text-embedding-3-small"

# AI Gateway API key (optional alternative to OPENAI_API_KEY for match explanations)
# AI_GATEWAY_API_KEY="REPLACE_WITH_AI_GATEWAY_KEY"

# GPT model for match explanations (optional, default: gpt-4)
# GPT_MODEL="gpt-4"

# Next.js / app URL used by some auth callbacks
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Other common entries you may want to include:
# NEXTAUTH_URL="http://localhost:3000"
# NEXTAUTH_SECRET="REPLACE_WITH_A_LONG_RANDOM_SECRET"
